# CompletionFeedback message
# Publicado quando >70% de uma linha é completada (follow line), ou quando >70% do
# deslocamento go-to-XY a um warehouse de processo (distância inicial -> alvo).

float64 x    # Coordenada x do ponto final da linha / alvo do warehouse
float64 y    # Coordenada y do ponto final da linha / alvo do warehouse

